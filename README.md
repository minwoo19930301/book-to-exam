BookVideoToExam — 책 넘기는 영상을 받아 시험지로 만듭니다.

1. 넘기는 영상을 찍어 이메일로 보냅니다. 시험지 링크는 완성되면 답장합니다.
2. 예시 사이트에서 뷰어 / 객관식 / 빈칸 / 서술형을 풉니다.
3. 채점은 본인 AI API 키, 이전 키 임시 사용, 또는 `/api/grade-temp` 대기 채점입니다.
4. MCP는 `/api/mcp/<임시id>` 입니다.
