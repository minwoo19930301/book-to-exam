# 통합 기록

- 원본: `minwoo19930301/smart-textbook-pipeline`
- 원본 마지막 커밋: `56bca26` (전체 SHA는 subtree 통합 커밋에 기록)
- 통합 위치: `tools/smart-textbook-pipeline/`
- `git subtree add`를 squash 없이 사용해 원본 커밋을 이 저장소 이력에 보존했습니다. 원본 MIT 라이선스는 이 디렉터리의 `LICENSE`에 유지됩니다.
- 통합 후 변경: Python 패키지 import와 Swift OCR 소스·캐시 경로를 현재 작업 디렉터리와 무관하게 실행하도록 보정했습니다.
- 이 모듈은 교재 준비용 보조 도구입니다. 운영 웹앱의 자동 업로드·시험 생성 API로 연결했다고 주장하지 않습니다.
